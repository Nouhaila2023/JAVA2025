package Tema4.EjercicioPropuestos.Pizza;

public class Pizza {
    /***
     * Pizza: tamaño, estado, tipo.
     * totalPedidas (static), totalServidas (static).
     * sirve, getters y setters, toString.
     */
    enum tamaño {pequina, mediana, grande};
    enum tipo {vegetariana, sin_gluten, gluten};
    enum estado {preparada, en_preparacion, servida};





}
